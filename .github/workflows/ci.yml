name: test

on:
  push:
    branches: ['**']
  pull_request:
    branches: ['**']

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Print time
        run: |
          echo "Event type: ${{ github.event_name }}"
          echo "time: $(date '+%Y-%m-%d %H:%M:%S %Z')"
